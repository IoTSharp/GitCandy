@model RepositoryFormViewModel
@{
    ViewData["Title"] = "Create repository";
}
<header class="page-heading"><div><h1>Create repository</h1><p>Create an empty bare repository, import a remote, or fork an existing repository.</p></div></header>
<partial name="_Form" model="Model" />
